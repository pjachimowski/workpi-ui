import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="WorkPi/Components/Dialog/Dialogs" />

# Dialogs

TODO

<figure>

```typescript
const dialogCtrl = useDialogTarget();
useEffect(() => {
  dialogCtrl.showDialog(createDialog(<LoginDialog></LoginDialog>));
}, []);
```

<figcaption>In this example a dialog is shown when a component is mounted.</figcaption>

</figure>